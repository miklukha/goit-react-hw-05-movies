import { SiteHeader } from './Header.styled';
import { Navigation } from 'components/Navigation';

export function Header() {
  return (
    <SiteHeader>
      <Navigation />
    </SiteHeader>
  );
}
